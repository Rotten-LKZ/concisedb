
import JSONAdapter, { JSONAdapterOptions } from './adapters/JSONAdapter'
import JSONAdapterSync, { JSONAdapterSyncOptions } from './adapters/JSONAdapterSync'

import { Adapter, ConciseDb } from './async'
import { AdapterSync, ConciseDbSync } from './sync'

export {
  ConciseDb,
  ConciseDbSync,
  Adapter,
  AdapterSync,
  JSONAdapterOptions,
  JSONAdapterSyncOptions,
  JSONAdapter,
  JSONAdapterSync,
}

export default {
  ConciseDb,
  ConciseDbSync,
  Adapter,
  AdapterSync,
  JSONAdapter,
  JSONAdapterSync,
}
